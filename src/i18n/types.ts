export type Messages = {
  home: {
    title: string;
    description: string;
  };
  notFound: {
    message: string;
    buttonText: string;
  };
  articles: {
    nextArticle: string;
    prevArticle: string;
  };
};
